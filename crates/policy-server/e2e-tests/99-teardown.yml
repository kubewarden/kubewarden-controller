name: Policy Server end-to-end tests teardown
vars:
  pidfile: /tmp/policy-server.pid

testcases:
- name: Stop policy-server
  steps:
  - script: |
      kill "$(< "{{ .pidfile }}")"
      rm {{ .pidfile }}
