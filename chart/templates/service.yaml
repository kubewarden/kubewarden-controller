apiVersion: v1
kind: Service
metadata:
  name: {{ include "chimera-controller.fullname" . }}-metrics-service
  labels:
    {{- include "chimera-controller.labels" . | nindent 4 }}
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https
  selector:
    {{- include "chimera-controller.selectorLabels" . | nindent 4 }}
